# Admin IP Whitelist
# Add your IPs here. One per line.
# Example:
# allow 203.0.113.1;      # Home IP
# allow 198.51.100.0/24;  # Office network
# allow 192.0.2.50;       # VPN IP

# IMPORTANT: Add your IPs above this line
# Default: deny all (uncommented in nginx.conf)

# Localhost (for server access)
allow 127.0.0.1;
allow ::1;
allow 110.226.183.76;
deny all;
