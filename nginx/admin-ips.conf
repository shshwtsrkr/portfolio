# Admin IP list used by nginx map (value 1 means allowed).
# Add your IPs here. One per line in `<ip_or_cidr> 1;` format.
# Example:
# 203.0.113.1 1;      # Home IP
# 198.51.100.0/24 1;  # Office network
# 192.0.2.50 1;       # VPN IP

# Localhost (for server access)
127.0.0.1 1;
::1 1;
110.226.183.76 1;
