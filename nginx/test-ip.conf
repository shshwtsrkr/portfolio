# Test file to check if real IP is working
location /test-ip {
    add_header Content-Type text/plain;
    return 200 "Remote IP: $remote_addr\nX-Forwarded-For: $http_x_forwarded_for\nCF-Connecting-IP: $http_cf_connecting_ip\n";
}
